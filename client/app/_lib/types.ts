export type UserDetails = {
  id: string | null;
  username: string | null;
};

export type LoginInfo = {
  username: string;
  password: string;
};

export type RegInfo = {
  username: string;
  password: string;
};
export type LinkDetails = {
  id: number;
  long: string;
};
